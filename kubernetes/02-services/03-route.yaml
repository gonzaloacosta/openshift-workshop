apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: myapp
  name: myapp
  namespace: workshop
spec:
  host: myapp-workshop.apps.ocp4.labs.semperti.local
  port:
    targetPort: http
  to:
    kind: Service
    name: myapp
    weight: 100
  wildcardPolicy: None
